<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% if locals and locals.news_list then %>
        <% for i, news in pairs(locals.news_list) do %>
            
            <% render("applications.themes.default.assets.widgets.card") %>
            
        <% end %>
    <% else %>
        <div class="col-span-full text-center py-12">
            <p class="text-gray-400 text-lg">📰 Aucune actualité disponible pour le moment</p>
        </div>
    <% end %>
</div>